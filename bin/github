#!/bin/bash

# repo=$(gh repo list --no-archived --json sshUrl --jq '.[].sshUrl' -L 400 $1 |fzf)
repo=$(cat $HOME/.local/github_repos.txt | fzf)
git -C $PROJECTS clone $repo
cd $PROJECTS/$(basename $repo .git)
pwd
$PROJECTS/base/bin/set_git_config.sh
